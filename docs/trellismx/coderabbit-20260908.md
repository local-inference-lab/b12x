# TrellisMX P8 review dispositions

Scope: native procedural-MCG E4M3/UE8M0-32 routed MoE with coupled H512/H128 transforms for the GLM-5.3-Flash TP4 overlay. These dispositions cover B12X PR342 and companion vLLM PR714 at review heads `ac8ef2ca23ab1f5bb45a94976b36a3ded0006bf1` and `558a5fed8c9a88c90993e2d5bb2497fb47a2c04b`.

Implemented: remove dead pre-H128 shared stores/barrier; Int64 route/output extents and offset products with a live >2^31 test; explicit codebook selection independent of environment; validate scales/transform before placeholder substitution; share procedural MCG decode implementation; reject unsupported TP2; bind scratch geometry to dispatched direct/grouped owner; reject incompatible grouped flags; distinguish H128 FC1 class name from identity FC1; context-independent transform and compile-key prose; explicit evidence failures under -O; per-arm audit flags/counts; bind historical comparison by hash and narrow equality claim to tokens/window order; remove unbound historical scalar instead of conflating FP8 with NVFP4; document corpus/role manifest and research-only status; narrow current identity-verification claim to the fields actually checked; TP4/DCP4 launcher admission; bounded numeric input strings; clean pinned CPU dependency check; skip optional B12X-dependent vLLM tests; register/cache TrellisMX environment opt-in; release persistent DCP workspace caches on teardown.

Deferred with source reasons:
- Consolidating the CTA/grid-owned H512 input traversal with `MoEDynamicKernelBackend._store_p8_full_coupled_input_row` in `b12x/moe/_shared/kernels/dynamic.py`, which materializes H512/suh/H128 input into E4M3 payload and UE8M0 scales: the helper advances by input_warps_per_token, while the inline input traversal spans CTA/grid-owned H512 units. A direct replacement changes traversal ownership; requires a separately designed equivalence test. Floating-point arithmetic and traversal ownership are retained; direct-input integer offsets are hardened separately below.
- Unifying entire FC1 classes: identity and H128 paths have different intermediate width, route bounds, epilogue transform and rounding. Renamed the H128-local variant to remove ambiguous class identity; numerical consolidation is not a safe mechanical fix.
- Routed-output weight/scale staging-helper extraction: duplicated code is equivalent and valid; no dead stores/barrier identified there. Deferred code-motion refactor to keep GPU validation focused on changed behavior.
- prequantization diagnostic carrier refactor: existing explicit diagnostic wrapper binds the carrier, and production has the diagnostic flag disabled. Removing its trace facility would remove a numerical investigation tool; no production numerical change made.
- Low-value chronology cleanup outside touched invariants: deferred; not serving correctness or speed work.

These deferred suggestions are not represented as implemented or resolved. GPU/source tests and updated PR descriptions must distinguish production overlay B12X base `e8ad299b174f16e2e8fb5879bea272f4efbb53f2` from the B12X PR construction base `6483963275dcf32eb2eec6d100e644d1ea647ed6`. The companion vLLM construction base is `9a6b4fb3a6f5598fd2fb68cf0de92bfe145294c1` on `dev/jovian-judgement`; these are source-composition identities, not fresh GPU qualification.

## Review of the latest comments and measured alternatives

Reviewed with OpenAI Codex and the local GLM-5.3-Flash TrellisMX model against B12X `36d5d55a9da264e35049b242e489ef320304919c` and companion vLLM `713d6371a808c877cb703105800e948ecc1cf555`. Decisions use existing KLD receipts, scoped source analysis, and the explicitly identified local GPU measurements.

| Proposal | Decision | Evidence and boundary |
| --- | --- | --- |
| Grouped FC1 register packing | Reject promotion; keep implementation local and absent from this PR | Separate exploratory ABBA image comparison: 32K prefill 7421.5 to 7368 tokens/s (-0.72%); both paired differences negative. Exact component outputs and two-CTA eligibility did not produce the predeclared 5% practical benefit. See `register-packing-check-20260908.json`. |
| Earlier `p8_coupled_topk.py` dead-store/barrier removal | Retain | `ac8ef2ca` removes four pre-H128 stores with no consumer and their barrier. It is distinct from grouped FC1 register packing; the register experiment cannot attribute a regression to this reducer cleanup. Existing reducer proxy evidence remains a correctness reference, not a production speed claim. |
| Undefined input-traversal labels, [B12X PR342 comment 3960570756](https://github.com/local-inference-lab/b12x/pull/342#discussion_r3960570756) | Accept | Name the CTA/grid-owned H512 input traversal and routed-output weight/scale staging directly. Documentation only. |
| Direct-input duplicate offset widening, [B12X PR342 review 5144992490](https://github.com/local-inference-lab/b12x/pull/342#pullrequestreview-5144992490) | Implement narrowly | `_p8_direct_input_block_offsets` is shared by the inline production store path and live GPU probe. Widen traversal bounds, indices and scaled offsets before multiplication; preserve the floating-point transform prefix and traversal ownership. Tests write actual payload and scale locations above 2^31 and replay a graph. K4/K5 M16 outputs and all profiled resources match the baseline. See `direct-input-offset-check-20260908.json`. No full-model KLD or speed claim. |
| `GLM53_MXFP6_H16_ALL` metadata redesign | Defer; not represented as resolved | This code was carried in imported source `7093ad77849cf181bcf0c30b897c54fd32dac40e`, not inherited unchanged from the PR construction base or current target base. It is gated by `quant_recipe == "w6a8_mx"`; the native P8 constructor passes `"w4a8_trellis"`. Changing unrelated MXFP6 artifact interpretation is outside this measured P8 correction. |
| vLLM companion pin | Keep source recipes consistent with the accepted B12X successor | Latest vLLM review of `713d6371` generated no actionable comments. Change dependency pins together; preserve published-image recipes and historical evidence identities. |
| Existing KLD evidence | Preserve original values, KV labels and research-only status | `evidence-r27-20260908/README.md` distinguishes audited historical/current image, backend and topology. Current DCP4 NVFP4/FP8 means are 0.0350078183/0.0310574767; retained DCP1 NVFP4/FP8 means are 0.0341811459/0.0318077613. These external receipts do not qualify this PR head or the register experiment. |

The native direct owner is selected at at most 16 tokens (`p8_native_kernel.py`); vLLM passes `prefill_chunk_tokens=0`. A production scheduler budget of 4096 is not a universal internal chunk guard. The integer fix addresses a latent generic large-offset hazard without claiming it caused a measured production failure.

The CuTe object-cache key includes a package source-content fingerprint (`b12x/_lib/compiler.py`). A new process after source changes gets a distinct binary identity; this validation also used fresh per-container cache directories. No running-service hot patch was performed. The original serving image remains selected.

## Follow-up review 5146498371

Codex and the local TrellisMX reviewer agreed to qualify the review links, spell out the baseline image digest, clear the offsets output before graph replay, and reject the unsupported grouped-M16 constructor before its nonexistent module could be imported. The direct and grouped-M64 implementations and floating-point math are unchanged. The stronger replay test passed both live GPU cases with no skips; `offsets_rearmed_followup` in `direct-input-offset-check-20260908.json` records its separate image, command and source hashes. The original two-test receipt and source hashes remain identified as the earlier run, before offsets rearming. No new KLD or performance result is claimed.
